# Screenshots

Drop screenshots here for use in the main README + future docs site.

Expected files (referenced from `/README.md`):

- `dashboard.png` — `/studio/dashboard/` hero shot at ~1440×900, EN or ES depending on which captures the look best. Should show: greeting, 4 stat cards with sparklines, recent posts list, activity feed populated.
- `editor.png` — `/studio/posts/<pk>/edit/` showing the Editor.js + persistent block toolbar + rail.
- `homepage-builder.png` — `/studio/homepage/` with a few blocks drag-and-drop reorderable.
- `theme.png` — `/studio/theme/` with iframe preview alongside the token controls.

When updating, keep file size under 600KB (use TinyPNG / squoosh for PNG, or convert to WebP).
