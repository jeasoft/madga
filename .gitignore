# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.Python

# Virtual envs
.venv/
venv/
env/

# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
miscoreblog.sqlite3
miscoreblog.sqlite3-journal
media/
staticfiles/
miscoreblog_media/
miscoreblog_staticfiles/

# uv / pip
.uv/

# OS
.DS_Store
Thumbs.db

# IDE / editors
.vscode/
.idea/
*.swp
*~

# Claude / AI tooling — keep settings shared, ignore local-only state
.claude/settings.local.json
.claude/state/
.claude/cache/

# Build artefacts (Tailwind output IS committed — runtime needs it)
node_modules/

# Python build/dist output (publish workflow rebuilds these)
build/
dist/
*.egg-info/

# Test / coverage
.pytest_cache/
.coverage
htmlcov/

# Sibling sites that consume MADGA — they belong in their own repos.
# Kept on disk for local dev convenience but never tracked here.
/miscoreblog/
