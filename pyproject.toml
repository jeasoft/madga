[project]
name = "madga"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "django>=5.0,<6",
    "django-allauth[headless]>=65.16.1",
    "django-ninja>=1.3",
    "pillow>=10",
    "python-slugify>=8",
]
